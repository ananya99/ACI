# @package _global_

hydra:
  sweeper:
    parametrization:
      # env.agents.agent_predator.eyes.eye.fov:
      #   init: ${${parent:}}
      #   lower: 1
      #   upper: 100
      env.agents.agent_prey.eyes.eye.fov:
        init: ${${parent:}}
        lower: 1
        upper: 100