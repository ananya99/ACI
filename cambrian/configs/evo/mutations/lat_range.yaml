# @package _global_

hydra:
  sweeper:
    parametrization:
      env.agents.agent_predator.eyes.eye.lat_range:
        init: ${${parent:}}
        lower: -90
        upper: 90
      env.agents.agent_prey.eyes.eye.lat_range:
        init: ${${parent:}}
        lower: -90
        upper: 90
