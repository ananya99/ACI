# @package _global_

hydra:
  sweeper:
    parametrization:
      env.agents.agent_predator.eyes.eye.resolution:
        init: ${${parent:}}
        lower: 1
        upper: 100
        integer: true
      env.agents.agent_prey.eyes.eye.resolution:
        init: ${${parent:}}
        lower: 1
        upper: 100
        integer: true
